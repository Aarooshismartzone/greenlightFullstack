export default function WhyChooseUsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <section>
        <title>Why Choose Greenlight</title>
      <div>
        {children}
      </div>
    </section>
  );
}
