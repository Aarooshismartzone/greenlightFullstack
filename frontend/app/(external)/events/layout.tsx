export default function OurMarketLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <section>
        <title>Our Market</title>
      <div>
        {children}
      </div>
    </section>
  );
}
